# Reviews

Keep the latest review/QA readiness artifact at:

`review.md`

Use frontmatter with `version`, `status`, `updated`, and `canonical: true`.
Archive milestone snapshots under `archive/` only when useful.
