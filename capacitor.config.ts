import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.kangsungbae.orbitslash",
  appName: "Orbit Slash",
  webDir: "dist",
};

export default config;
